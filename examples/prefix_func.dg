[s.len > 5 + 1 * 2 && s.len <= 15 || true]
int[] prefix_func(string s) {
	string test_str = "hey str"
	int[] p = int[s.len]
	int k = (0 + 0 * 0)
	int i
	i = 0
	for i = 1; i < s.len; i++ {
		[prob == 10 && s[i] == 'b']
		while k > 0 && s[i] != s[k] {
			k = p[k-1]
		}
		[prob == 80]
		if s[i] == s[k] {
			k++
		}
		p[i] = k
	}

	return p
}
