int prop_lookup(int i, string s) {
    string[] str_arr = string[2]
    [prob = 50;]
    if s.len > 2 && s.len < 5 || str_arr.len == 1 {
        return i
    }

    int size = 50
    int[] s_arr = int[size]
    int j
    for j = 0; j < size; j++ {
        s_arr[j] = j
    }

    if s_arr.len > 50 {
        return s_arr.len
    }

    return s_arr.len + s_arr[j-1]
}
